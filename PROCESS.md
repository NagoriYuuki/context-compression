# PROCESS

## 工具和分工

本次实现使用 Codex 进行方案 review、代码实现、测试和本地验证。用户负责提出需求、审阅方案和分阶段 commit，并由用户决定何时 push。中间件和测试未调用真实 LLM API 或外部摘要服务。

## 关键决策记录

初始方案包含任务状态提取、保护等级、近期 Token 比例、多个压缩阶段和 SystemReducer。review 后删除这些职责，收敛为消息协议校验、ToolRound 分组、TokenCounter、一次摘要和确定性 fallback。原因是作业核心要求是预算、Tool 一致性、失败路径和可验证性，复杂策略会增加实现面并引入无法证明的语义判断。

代表性判断：不使用关键词自动提取 goal、failure、pending，因为 Tool Result 中的关键词可能造成误判；改为使用调用方显式的 `Protected/Tags`，并通过 fixture 验证关键消息仍存在。

## 实现检查点

- 方案：已完成精简并提交；
- 数据模型和深拷贝：已完成并有单元测试；
- TokenCounter：已完成并有边界测试；
- 输入校验和 ToolRound：已完成并覆盖孤立 Result、重复 ID 和未完成轮次；
- 摘要接口和 FakeSummarizer：已完成并覆盖失败模式；
- Middleware 主流程：已完成并覆盖清理、摘要、fallback、CannotFit；
- 长会话 fixture 和文档：本次提交完成；
- 验证状态：`go test ./...`、`go test -race ./...`、`go vet ./...` 已通过。

## AI 建议的修改和验证

曾发现摘要失败 fallback 使用固定长度后，在预算极小时仍然可能超限。实现改为根据当前总 Token 预算再次选择可接受的头尾长度，必要时使用确定性占位内容；随后新增摘要失败测试并通过。

## 补充功能演示（2026-09-15）

代表性会话节选（只保留本轮相关用户原话，其他讨论已删节）：

> 用户：还有演示功能的测试，我现在想要的是后者
>
> 用户：并且我提交的作业里面你看一下是否也需要后者？
>
> [删节：需求复核与五个演示场景的建议]
>
> 用户：补上

本轮据此扩展已有 `demo_test.go`，用一份订单超时排查样例展示不压缩、清理工具结果、历史摘要、摘要失败降级和 CannotFit。每个场景打印前后消息、实际摘要调用次数、来源和任务信息，并用断言防止展示内容与实际路径不一致。使用的项目约束包括：保持方案轻量，按组件提交本地 commit，由用户 review 和 push。本轮未使用项目 `AGENTS.md` 或 Skill。

对 AI 原实现的纠错：先增加针对性回归测试，实际复现“带 log 标签的最新请求被 fallback 删除”和“JSON 被占位文本替换后仍标为 JSON”。修复后测试通过，提交为 `580855e`。同时修正未经证实的“模型已处理结果”占位说明，以及未修改内容却报告 `fallback_applied` 的诊断。

本轮确实通过上一轮提供的状态摘要恢复工作。恢复摘要相关原文节选（其余审计内容已删节）：

> Only tool-result clearing occurs; FakeSummarizer is not invoked.
>
> Never push; user will push.

恢复后重新读取需求、代码、已有演示和 Git 状态，确认已有演示只覆盖工具清理，`.gitignore` 与 `demo_test.go` 为未跟踪文件；在已有演示上补充场景，并保留未关联本次修改的 `.gitignore`。通过实际运行逐一确认五条路径，避免仅依赖恢复摘要中的结论。

本轮提交前检查点：已完成修复、长会话、五个演示及 README；已通过构建、全量测试、race 检查和 vet，并验证单独运行摘要场景与完整 JSON 输出。待办为用户 review、push，以及根据真实记录补充实际投入时间和历次使用的具体模型版本。过程记录不以自动化检查通过代替人工 review。

### 演示展示方式返工

用户进一步指出：“我需要一个明确的完整原文，以及完整的进行上下文压缩之后的内容”“你这个冗杂信息还是太多了”。据此移除默认报告里的预览表、重复任务信息和诊断清单，改为完整的压缩前后会话。实际运行结果保存为 `DEMO.md`，可直接阅读；详细元数据保留为可选输出。五个场景原有的验证断言继续执行。
